name: Try to Create OCI VM
# It will run every 10 minutes, offset from the top of the hour.
on:
  schedule:
    - cron: "3,13,23,33,43,53 * * * *"
  # Allows you to manually run this workflow from the Actions tab
  workflow_dispatch:
env:
  COMPARTMENT_ID: ${{ secrets.OCI_COMPARTMENT_ID }}
  IMAGE_ID: ${{ secrets.IMAGE_ID }}
  SUBNET_ID: ${{ secrets.OCI_SUBNET_ID }}
  AD_NAME: ${{ secrets.AD_NAME }}
  OCI_CLI_REGION: ${{ secrets.OCI_CLI_REGION }}
  OCI_CLI_USER: ${{ secrets.OCI_CLI_USER }}
  OCI_CLI_TENANCY: ${{ secrets.OCI_CLI_TENANCY }}
  OCI_CLI_FINGERPRINT: ${{ secrets.OCI_CLI_FINGERPRINT }}
  OCI_CLI_KEY_CONTENT: ${{ secrets.OCI_CLI_KEY_CONTENT }}
  SUPPRESS_LABEL_WARNING: "True"

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Cache pip packages
        id: cache-pip
        uses: actions/cache@v4
        with:
          path: ~/.local
          key: ${{ runner.os }}-pip-oci-cli-v1

      - name: Add OCI CLI to path
        run: echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Setup OCI CLI and Try to Create VM
        id: create_vm
        # This forces the job to succeed so the cache will save.
        continue-on-error: true 
        run: |
          set -o pipefail

          pip install oci-cli

          mkdir -p ~/.oci
          echo "${{ secrets.OCI_CLI_KEY_CONTENT }}" > ~/.oci/oci_api_key.pem
          chmod 600 ~/.oci/oci_api_key.pem

          echo "[DEFAULT]" > ~/.oci/config
          echo "user=${{ secrets.OCI_CLI_USER }}" >> ~/.oci/config
          echo "tenancy=${{ secrets.OCI_CLI_TENANCY }}" >> ~/.oci/config
          echo "fingerprint=${{ secrets.OCI_CLI_FINGERPRINT }}" >> ~/.oci/config
          echo "key_file=~/.oci/oci_api_key.pem" >> ~/.oci/config
          echo "region=${{ env.OCI_CLI_REGION }}" >> ~/.oci/config
          echo "${{ secrets.SSH_PUBLIC_KEY }}" > /home/runner/my_ssh_key.pub

          MAX_ATTEMPTS=3
          OCI_OUTPUT=""

          for attempt in $(seq 1 "$MAX_ATTEMPTS"); do
            echo "OCI launch attempt ${attempt}/${MAX_ATTEMPTS}..."

            ATTEMPT_OUTPUT=$(oci compute instance launch \
              --compartment-id "$COMPARTMENT_ID" \
              --availability-domain "$AD_NAME" \
              --shape "VM.Standard.A1.Flex" \
              --shape-config '{"ocpus":4,"memoryInGBs":24}' \
              --subnet-id "$SUBNET_ID" \
              --image-id "$IMAGE_ID" \
              --ssh-authorized-keys-file "/home/runner/my_ssh_key.pub" \
              --assign-public-ip true \
              --display-name "coolify-vm" \
              --boot-volume-size-in-gbs 200 2>&1 || true)

            OCI_OUTPUT="$(printf '%s\n--- Attempt %s/%s ---\n%s' \
              "$OCI_OUTPUT" \
              "$attempt" \
              "$MAX_ATTEMPTS" \
              "$ATTEMPT_OUTPUT")"

            if echo "$ATTEMPT_OUTPUT" | grep -q '"lifecycle-state": "PROVISIONING"'; then
              echo "Success! OCI returned lifecycle-state PROVISIONING."
              break
            fi

            if echo "$ATTEMPT_OUTPUT" | grep -q 'Out of host capacity'; then
              echo "OCI capacity unavailable for VM.Standard.A1.Flex in this availability domain. This is the expected retry state."
              break
            fi

            echo "OCI launch attempt did not return success or expected capacity error. Raw OCI output:"
            printf '%s\n' "$ATTEMPT_OUTPUT"

            if [ "$attempt" -lt "$MAX_ATTEMPTS" ]; then
              sleep $((attempt * 30))
            fi
          done

          # Store the log as a GitHub step output
          echo "log_data<<EOF" >> $GITHUB_OUTPUT
          echo "$OCI_OUTPUT" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

          # Check the log content for an error string.
          if echo "$OCI_OUTPUT" | grep -q '"lifecycle-state": "PROVISIONING"'; then
            echo "Success! 'PROVISIONING' state found in output."
            exit 0
          elif echo "$OCI_OUTPUT" | grep -q 'Out of host capacity'; then
            echo "Capacity unavailable. Keeping the workflow in retry mode without sending Feishu notification."
            exit 1
          else
            echo "Error: Success string not found in OCI output. Failing the step."
            exit 1
          fi

      - name: Send Feishu Notification
        if: always() && (steps.create_vm.outcome == 'success' || !contains(steps.create_vm.outputs.log_data, 'Out of host capacity'))
        env:
          FEISHU_WEBHOOK_URL: ${{ secrets.FEISHU_WEBHOOK_URL }}
          FEISHU_WEBHOOK_SECRET: ${{ secrets.FEISHU_WEBHOOK_SECRET }}
          CREATE_VM_STATUS: ${{ steps.create_vm.outcome }}
          CREATE_VM_LOG: ${{ steps.create_vm.outputs.log_data }}
        run: |
          python3 - <<'PY'
          import base64
          import hashlib
          import hmac
          import json
          import os
          import time
          import urllib.error
          import urllib.request

          webhook_url = os.environ["FEISHU_WEBHOOK_URL"]
          secret = os.environ.get("FEISHU_WEBHOOK_SECRET", "")
          status = os.environ["CREATE_VM_STATUS"]
          log_data = os.environ.get("CREATE_VM_LOG", "")
          run_url = f"{os.environ['GITHUB_SERVER_URL']}/{os.environ['GITHUB_REPOSITORY']}/actions/runs/{os.environ['GITHUB_RUN_ID']}"

          if len(log_data) > 12000:
              log_data = f"{log_data[:12000]}\n\n...[truncated]"

          payload = {
              "msg_type": "post",
              "content": {
                  "post": {
                      "zh_cn": {
                          "title": f"OCI VM Status: {status}",
                          "content": [
                              [{"tag": "text", "text": "Attempt to create A1.Flex VM."}],
                              [{"tag": "text", "text": f"Status: {status}"}],
                              [{"tag": "a", "text": "View GitHub Actions run", "href": run_url}],
                              [{"tag": "text", "text": f"Log Output:\n{log_data}"}],
                          ],
                      }
                  }
              },
          }

          if secret:
              timestamp = str(int(time.time()))
              string_to_sign = f"{timestamp}\n{secret}".encode("utf-8")
              sign = base64.b64encode(
                  hmac.new(string_to_sign, digestmod=hashlib.sha256).digest()
              ).decode("utf-8")
              payload["timestamp"] = timestamp
              payload["sign"] = sign

          request = urllib.request.Request(
              webhook_url,
              data=json.dumps(payload, ensure_ascii=False).encode("utf-8"),
              headers={"Content-Type": "application/json; charset=utf-8"},
              method="POST",
          )

          try:
              with urllib.request.urlopen(request, timeout=20) as response:
                  body = response.read().decode("utf-8")
                  print(response.status)
                  print(body)
                  result = json.loads(body)
          except urllib.error.HTTPError as error:
              print(error.read().decode("utf-8"))
              raise

          if result.get("code") not in (0, None) or result.get("StatusCode") not in (0, None):
              raise SystemExit(f"Feishu webhook returned an error: {body}")
          PY
